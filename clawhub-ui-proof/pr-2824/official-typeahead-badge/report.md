# ClawHub UI Proof

Status: pass

Scenario: global search typeahead for `brave` on `http://localhost:3001` shows the official badge after `@local-corpus-stephanie-blanda` and before `/ brave-plugin`.
